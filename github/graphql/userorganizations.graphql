# Fetch first 10 organization that the user is an admin.
query UserOrganizations{
    viewer {
        organizations(
            first: 10
        ) {
            nodes {
                name,
                id,
                login,
            }
        }
    }
}