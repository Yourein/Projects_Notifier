# Fetch first 10 organization that the user is an admin.
query{
    viewer {
        organizations(
            first: 10
        ) {
            nodes {
                name,
                id,
                login,
            }
        }
    }
}