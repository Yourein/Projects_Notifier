# Fetch first 10 projects on an organization
query(
    $org_login: String
) {
    viewer {
        organization(
            login: $org_login
        ) {
            projectsV2(
                first: 10
            ) {
                nodes {
                    title,
                    id,
                    number,
                }
            }
        }
    }
}